# Progress Log

## 2026-07-13 · 启动与现状审查

- 确认项目是已上线的现有单页计算器，不是新项目或代码分叉。
- 检查当前快速开始区域、月度明细、localStorage、JSON 备份、CSV 导出及自动计算规则。
- 发现首屏同时暴露约 18 个输入项，且把“应缴基数、公司比例、实缴区间、合同期限”等专业模型直接交给普通用户填写。
- 确认本轮用户目标是减少输入和降低理解成本，不授权直接改变已确认的法律计算口径。

## 2026-07-13 · 产品方案形成

- 形成“事实问答 → 系统推定 → 结果确认 → 精算修正”的推荐主流程。
- 将免登录、本地保存、四类欠款合计、实缴抵扣、双倍工资规则及验证方法列为 LOCKED。
- 将引导式首屏、场景选择、政策预设、批量粘贴和异常月编辑列为 PROTECTED，等待用户确认后实施。
- 生成结构化需求与 RED-GREEN-REFACTOR 实施计划。

## 2026-07-13 · 用户确认与实施启动

- 用户确认继续推进“引导模式默认方案”。
- 将默认引导、三项基础事实、按事项展示、显式推定、汇总优先和折叠精算明细转为 LOCKED。
- 本阶段明确排除未确认的地区政策库和缴费记录粘贴，不使用无来源比例。
- 开始按 RED-GREEN-REFACTOR 实施第一阶段。

## 2026-07-13 · 引导模式实现

- 先添加引导流程和事项隔离失败测试，确认 RED 后实施。
- 默认流程已改为 `basic → scenario → questions → review → results`。
- 初始只展示入职日期、统计截止日期、合同月薪；后续问题按四类事项条件出现。
- 合同开始日、社保与公积金实缴开始月显示“系统推定”并允许修改。
- 结果页先展示选中事项汇总与异常月份，逐月台账折叠到“查看精算明细”。
- 未选择事项从逐月生成、结果卡片、CSV 行合计和总计中排除。
- 未引入无来源地区政策比例；社保、公积金公司比例继续由用户明确填写。
- 引导测试与生产构建已通过；完整回归测试更新中。
- 完整回归测试 4/4 通过，`git diff --check` 通过。
- 本地真实页面完成桌面和 390px 移动端验收：页面无横向溢出，精算表格仅在自身容器滚动，控制台无错误或警告。
- 源码提交 `4c4df57` 已推送，Sites v8 私有生产部署成功：`https://xinbao-qingsuantai.kunkun8964.chatgpt.site`。

## 2026-07-13 · 实缴金额与比例提示增强

- 用户确认社保、公积金模块应把公司实际缴纳金额设为主要输入，并由系统提示法规比例范围。
- 核对官方口径后锁定：公积金单位比例 5%–12%；社保养老单位部分 16%，社保合计比例因地区和险种而异，不展示虚假的全国统一总区间。
- 先添加失败测试，再完成实缴金额主输入、比例提示、快捷比例和折叠测算基数设置。
- 回归测试 5/5 通过，生产构建和 `git diff --check` 通过。
- 桌面和 390px 手机宽度真实页面验收通过：比例说明卡片无溢出，折叠测算基数正常，控制台无错误或警告。
- 功能提交 `5555559` 已推送，Sites v9 私有生产部署成功，继续使用原站点地址。

## 2026-07-13 · 合同问题简化

- 用户反馈“劳动合同开始日 / 系统推定”难以理解。
- 确认该字段不参与双倍工资公式，决定从引导问题、确认页和规则结果中移除。
- 新问题改为“合同上写的最后一天”，并解释其等于劳动合同期满日。
- 旧数据结构继续保留 `contractStart` 以兼容历史 JSON，但页面不再要求填写。
- 先添加失败测试后实施，完整回归测试 6/6、生产构建和 `git diff --check` 通过。
- 功能提交 `9d5e9d1` 已推送，Sites v10 私有生产部署成功，继续使用原站点地址。

## 2026-07-13 · 合同月薪金额卡

- 用户反馈原合同月薪输入像表格单元格，视觉层级和美观度不足。
- 保持 `contractPay` 数据和计算逻辑不变，改为金额主体、币种徽标和“元/月”辅助信息的输入卡。
- 增加“劳动合同约定的税前月工资”解释，并隐藏数字输入原生调节箭头。
- 先添加失败测试后实施，完整回归测试 7/7、生产构建和 `git diff --check` 通过。
- 桌面端聚焦状态和 390px 手机宽度验收通过，页面无横向溢出，控制台无错误或警告。
- 功能提交 `1bb5bbd` 已推送，Sites v11 私有生产部署成功，继续使用原站点地址。

## 2026-07-13 · 缴费比例自动反推与最低比例兜底

- 用户确认：没有提交公司缴费基数时按合同月薪作为缺省测算基数，并由系统根据公司实际月缴金额反推比例。
- 锁定公式：`反推实缴比例 = 公司实际月缴金额 ÷ 有效测算基数`，`系统采用比例 = max(反推实缴比例, 当地最低比例)`。
- 社保最低比例默认 16%，明确仅是养老保险单位部分的保守基线；公积金最低单位比例默认 5%，两者都允许按当地规则手工修改。
- 界面新增“反推比例 → 最低比例 → 系统采用比例”可视计算过程，缴费基数仍保留折叠修改入口。
- 已先添加失败测试再实现；完整回归 8/8、生产构建和 `git diff --check` 均通过。
- 桌面端与 390px 手机宽度真实页面验收通过：两张比例计算卡片均无横向溢出，控制台无错误或警告。
- 功能提交 `4137334` 已推送，Sites v12 私有生产部署成功，继续使用原站点地址。

## 2026-07-14 · 五险申报基数自动测算

- 用户明确纠正原产品口径：社保不能只用养老保险比例，应覆盖养老、失业、工伤、生育和医疗五项公司费率。
- 社保主输入从“公司实际每月缴纳金额”改为“公司实际申报缴费基数”。
- 锁定公式：`五险合计费率 = 五项公司费率之和`，`实际缴纳 = 实际申报基数 × 五险合计费率`，`应缴 = 应缴测算基数 × 五险合计费率`，`漏缴 = max(应缴 - 实际缴纳, 0)`。
- 默认展示用户截图中的 14%、2%、0.8%、0.6%、11.5% 参考值，界面明确标注可修改且不代表杭州现行统一标准。
- 旧案例只有 `socialPaid` 时按新合计费率反推等值实际申报基数，迁移前后实际月缴金额保持一致。
- 已按 RED-GREEN 添加纯计算与界面契约测试；完整回归 15/15、Sites 生产构建、GitHub Pages 静态构建和 TypeScript 检查均通过，发布进行中。

## 2026-07-14 · 报销欠款与 A4 测算报告

- 新增“报销费用未支付”事项，用户只需填写尚未支付金额，可选填写事项说明。
- 报销提供“计入本次合计 / 仅在报告中记录”两种明确口径，默认计入合计；两种口径均在结果卡片和报告中可追溯。
- 当前测算合计、异常项目和报告拆分已统一接入报销金额；旧本地数据通过默认字段兼容，备份数据版本升级至 v7。
- 新增仅在打印时显示的 A4 报告，采用参考图的黑白编辑排版、几何纹样、高对比大金额、点线拆分、双倍工资依据、报销说明、报告编号与免责声明。
- 报告通过浏览器系统打印导出，用户可直接“另存为 PDF”，不上传工资或报销数据，也不伪造附件核验与二维码能力。
- 已按 RED-GREEN 补充报销口径、报告结构、打印隔离和服务端渲染测试；完整回归 17/17、生产构建、GitHub Pages 静态构建和 TypeScript 检查均通过。
- 功能提交 `e2b23cc` 已推送 GitHub；Sites v21 生产部署成功。GitHub Pages 的服务端 HTML 与客户端构建块均已核验包含报告标题、报销入口、两种计入口径和导出按钮。

## 2026-07-14 · 现代咨询/审计式报告升级

- 用户明确要求彻底移除花纹、插画、水印、渐变、装饰性线条和无意义图形，报告转为理性、精准、适合归档的现代中文专业文档。
- 报告重建为严格模块化结构：页眉身份区、标题与摘要、四列元数据、关键合计、语义化欠款构成表、双倍工资与报销双栏、报告说明、免责声明和页脚。
- 视觉仅使用黑、白、深灰和低饱和蓝灰强调色；中文使用现代无衬线系统字体，金额使用等宽数字并全部右对齐。
- 已删除几何纹样、圆形类别图标、点线连接、装饰箭头和虚拟条码；轻量实线仅承担表格分隔与模块边界功能。
- 已按 RED-GREEN 更新报告结构与反装饰契约测试；完整回归 17/17、生产构建、GitHub Pages 静态构建和 TypeScript 检查均通过，发布进行中。
- 功能提交 `fed4dfd` 已推送 GitHub；Sites v23 生产部署成功，线上报告已更新为现代专业版式。
